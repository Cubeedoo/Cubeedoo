self.onmessage=function(event){
		console.log('hi');
		console.dir(event);
		console.log(event);
 		// handle the message
		//var stuff = event.data;
		// and send it back to the main thread
		//postMessage(stuff);
};
